# ebash - Extended Bash Syntax Preprocessor/Executor

## Usage

Use ebash the same way you would use `source` or `bash`. Flags from the `bash` command to come to `ebash` as well, starting with `-C` since it is simpler
